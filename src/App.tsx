import React, { FunctionComponent } from 'react';

type User = {
  name: string;
  pinnedPages: number[];
  pagesOrder: number[];
};

export const App: FunctionComponent = () => {
  return (
    <div>
    </div>
  );
};
